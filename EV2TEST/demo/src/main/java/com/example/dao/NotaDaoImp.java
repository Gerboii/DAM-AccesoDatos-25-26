package com.example.dao;

import org.hibernate.Session;
import org.hibernate.Transaction;

public class NotaDaoImp {
    private Session session;
    private Transaction transaction;

    /*
     * TODO Método alumAsignatura() recibe una asignatura y devuelve el listado de alumnos con su nota
     * Método guardarNota() recibe las notas de la tabla y las guarda en BD
     * Método mostrarNota() recibe dniAlumno y asignatura, devuelve la nota.
     * OPCIONAL- Método mostrarEvaluacion() recibe dniAlumno, devuelve todas las notas asociadas.*/
}
